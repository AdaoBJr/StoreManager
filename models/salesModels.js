const { ObjectId } = require('mongodb');
const mongoConnection = require('./connection');

module.exports = {
  // createProduct,
  // findByName,
  // getAll,
  // getById,
  // update,
  // exclude,
};