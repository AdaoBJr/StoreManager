const { validateName } = require('./validateName');
const { validateQuantity } = require('./validateQuantity');
const { validateQuantitySales } = require('./validateQuantutySales');

module.exports = {
    validateName,
    validateQuantity,
    validateQuantitySales,
};
