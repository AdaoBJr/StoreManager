const express = require('express');

const router = express.Router();
const salesController = require('../controllers/productController');

// router.post('/', productController.createProduct);
module.exports = router;